{
	admin off
	auto_https off
}

:{$PORT:80} {
	root * /app
	php_server
	file_server

	encode gzip

	@php {
		path *.php
	}
	handle @php {
		php_server
	}

	handle {
		try_files {path} {path}/ /index.php?{query}
	}
}
